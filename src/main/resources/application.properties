# Server Port
server.port=8080

spring.datasource.url=jdbc:mysql://${DB_HOST:autovalue-autovalue.e.aivencloud.com}:${DB_PORT:20753}/defaultdb?sslMode=REQUIRED&serverTimezone=UTC
spring.datasource.username=${DB_USER:avnadmin}
spring.datasource.password=${DB_PASSWORD:AVNS__qZLN8lVtchGgN1fRHj}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Upload limits (if needed for images later)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
